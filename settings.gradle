pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
    }
}
rootProject.name = "se-tetris-team4-1"

// app 모듈 등록
include(":app")
project(":app").projectDir = file("app")

